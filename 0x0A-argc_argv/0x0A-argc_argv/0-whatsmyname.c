#include <stdio.h>

/**
 * main - print the name of the program
 * @argc: first parameter count arguments
 * @argv: second parameter arguments
 * Return: 0 if successful
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
